[project]
name = "shared_config"
version = "0.1.0"
description = "Shared configuration package"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"