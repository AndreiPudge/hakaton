# 🚀 Краткий мануал по Git

## ⚡ Основные правила

1. **НЕ коммитить в main** - только в свои ветки
2. **ВСЕГДА делать pull перед push** - избегаем конфликтов
3. **Коммитить часто** - маленькими осмысленными частями

## 🛠️ Алгоритм команд с нуля

### Первая настройка (делается один раз)
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваша@почта.com"

git clone git@github.com:AndreiPudge/hakaton.git
cd hakaton

# 1. Получить свежие изменения
git checkout main
git pull origin main

# 2. Создать ветку для задачи
git checkout -b feature/название-задачи

# 3. Работать и сохранять
git add .
git commit -m "Описание изменений"

# 4. Отправить на проверку (перед этим убедись что в main нет изменений!)
git pull origin main    # ← ВАЖНО: получить свежее!
git push origin feature/название-задачи

# Когда Pull Request приняли и вмержили в main
git checkout main
git pull origin main
git branch -d feature/название-задачи  # удалить локальную ветку (в конце)

# Статус и информация
git status                  # что изменилось
git log --oneline           # история коммитов
git branch                  # список веток

# Если что-то пошло не так
git restore файл            # отменить изменения в файле

# Ветки
git checkout ветка          # переключиться на ветку
git branch -d ветка         # удалить ветку
